const initialState = {
  episodesList: [],
  pending: false,
  error: null,
};

export const EpisodesReducer = (state = initialState, action) => {
  switch (action.type) {
    case value:
      return state;

    default:
      return state;
  }
};
